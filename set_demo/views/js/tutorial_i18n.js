/**
 * Next Order Discount demo — каталог переводов текстов туров.
 *
 * Демо сейчас работает только на русском (панель RU-only, см. panel.tpl), а русский —
 * язык-источник строк в tutorial_data_*.js, для него перевод не ищется. Поэтому каталог
 * пуст: при отсутствии перевода SLMDemo.tr() возвращает исходную (русскую) строку.
 *
 * Если понадобится вернуть переключатель EN/FR — заполните карты EN/FR, где ключ каждой
 * записи — ТОЧНАЯ русская строка шага из tutorial_data_admin.js / tutorial_data_front.js.
 */
(function () {
    var EN = {};
    var FR = {};

    window.SLM_TOUR_I18N = { en: EN, fr: FR };
})();
